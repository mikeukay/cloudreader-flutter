import 'package:flutter/material.dart';

class BodyWeb extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This function is not available on the web.'),
    );
  }
}
